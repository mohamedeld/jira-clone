import { Button } from "@/components/ui/button";
import Image from "next/image";

export default function Home() {
  return (
    <>
      <Button variant={"destructive"}>we</Button>
      <Button>we</Button>
      <Button variant={"secondary"}>we</Button>
      <Button variant={"teritary"}>we</Button>
    </>
  );
}
